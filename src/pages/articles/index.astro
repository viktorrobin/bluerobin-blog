---
import MainLayout from "@layouts/MainLayout.astro";
import { getCollection } from "astro:content";
import { CATEGORIES, DIFFICULTIES, SERIES } from "@/config";

// Get all published posts, sorted by date
const allPosts = await getCollection("blog", ({ data }) => !data.draft);
const sortedPosts = allPosts.sort((a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf());

const formatDate = (date: Date) => date.toLocaleDateString("en-US", { year: "numeric", month: "short", day: "numeric" });
---

<MainLayout title="Blog | BlueRobin Tech Blog" description="All articles on .NET, Kubernetes, LLM integration, and Domain-Driven Design">
  <div class="max-w-6xl mx-auto px-4 sm:px-6 lg:px-8 py-12">
    <header class="mb-12">
      <h1 class="text-3xl sm:text-4xl font-display font-semibold text-slate-900 mb-4">
        All Articles
      </h1>
      <p class="text-lg text-slate-600">
        Deep dives, cookbooks, and implementation guides from the BlueRobin project.
      </p>
    </header>

    <!-- Article Series -->
    <section class="mb-12">
      <h2 class="text-2xl font-display font-semibold text-slate-900 mb-6">
        Article Series
      </h2>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
        {Object.entries(SERIES).map(([key, series]) => {
          const borderColor = {
            neural: "border-neural-400",
            quantum: "border-quantum-400",
            aurora: "border-aurora-400",
            glow: "border-glow-400",
          }[series.color] ?? "border-neural-400";
          return (
            <a href={`/tags/${key}/`} class={`glass-card p-5 hover:shadow-lg transition-all hover:-translate-y-1 border-l-4 ${borderColor}`}>
              <h3 class="font-medium text-slate-800 mb-1">{series.title}</h3>
              <p class="text-sm text-slate-600">{series.description}</p>
            </a>
          );
        })}
      </div>
    </section>

    <!-- Browse by Category -->
    <section class="mb-12">
      <h2 class="text-2xl font-display font-semibold text-slate-900 mb-6">
        Browse by Category
      </h2>
      <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-4">
        {Object.entries(CATEGORIES).map(([key, cat]) => {
          const borderColor = {
            neural: "border-neural-400",
            quantum: "border-quantum-400",
            aurora: "border-aurora-400",
            glow: "border-glow-400",
          }[cat.color] ?? "border-neural-400";
          return (
            <a href={`/${key}/`} class={`glass-card p-5 transition-all border-l-4 ${borderColor}`}>
              <h3 class="font-medium text-slate-800 mb-1">{cat.title}</h3>
              <p class="text-sm text-slate-600">{cat.description}</p>
            </a>
          );
        })}
      </div>
    </section>

    {sortedPosts.length === 0 ? (
      <div class="glass-card p-12 text-center">
        <p class="text-slate-600 text-lg">
          Articles coming soon. The first article in the DDD series is being prepared.
        </p>
      </div>
    ) : (
      <div class="space-y-6">
        {sortedPosts.map((post) => {
          const categoryMeta = CATEGORIES[post.data.category];
          const difficultyMeta = DIFFICULTIES[post.data.difficulty];
          const seriesTags = post.data.tags.filter(t => t.endsWith("-series"));
          const regularTags = post.data.tags.filter(t => !t.endsWith("-series")).slice(0, 3);
          
          return (
            <article class="glass-card p-6 hover:shadow-lg transition-all">
              <div class="flex flex-col md:flex-row md:items-start gap-4">
                <div class="flex-1">
                  <a href={`/articles/${post.slug}/`}>
                    <h2 class="text-xl font-medium text-slate-800 hover:text-neural-600 transition-colors mb-2">
                      {post.data.title}
                    </h2>
                  </a>
                  
                  <p class="text-slate-600 mb-3 line-clamp-2">
                    {post.data.description}
                  </p>
                  
                  <div class="flex flex-wrap items-center gap-2">
                    {seriesTags.map((tag) => (
                      <a href={`/tags/${tag}/`} class="tag tag-series text-xs">{tag.replace("-series", "")}</a>
                    ))}
                    {regularTags.map((tag) => (
                      <a href={`/tags/${tag}/`} class="tag text-xs">{tag}</a>
                    ))}
                  </div>
                </div>
                
                <div class="text-sm text-slate-500 md:text-right space-y-1">
                  <time datetime={post.data.pubDate.toISOString()}>
                    {formatDate(post.data.pubDate)}
                  </time>
                  {post.data.readTime && (
                    <p class="text-xs">{post.data.readTime}</p>
                  )}
                </div>
              </div>
            </article>
          );
        })}
      </div>
    )}
  </div>
</MainLayout>
