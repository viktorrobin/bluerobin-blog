---
title: "Optimizing Kubernetes Images for Homelab Resources"
description: "Running a complex microservices stack on limited hardware. How we use .NET 10 Chiseled Ubuntu images and Native AOT to slash memory usage."
pubDate: 2026-01-10
updatedDate: 2026-02-28
author: "Victor Robin"
category: "infrastructure"
difficulty: "intermediate"
tags: ["kubernetes", "performance", "dotnet"]
readTime: "8 min"
toc: true
---

import Callout from '@components/Callout.astro';
import ImplementationNote from '@components/ImplementationNote.astro';
import CodeFile from '@components/CodeFile.astro';
import ExternalCite from '@components/ExternalCite.astro';

When I first configured my k3s cluster to run the full BlueRobin stack -- API gateway, three worker services, PostgreSQL, NATS, and a monitoring suite -- I ran straight into OOMKilled events within the first hour. My nodes had 8 GB of RAM each and the default .NET images were consuming over 250 MB per pod before the application even processed its first request. I spent that first weekend frantically reading container runtime metrics and wondering why `kubectl top pods` showed memory climbing relentlessly. The breakthrough came when I discovered Microsoft's Chiseled Ubuntu images and Native AOT compilation, which together slashed my per-pod footprint by nearly 90%. This article captures that entire optimization journey.

## Introduction

Our system runs on a modest Kubernetes cluster (K3s). We don't have infinite cloud RAM. When running 15+ pods (API, Workers, Databases, Monitoring), every megabyte counts.

Standard .NET container images are "safe" but bloated. They contain shells, package managers, and binaries we never use.

**Why Optimization Matters:**
- **Density**: Run more services on the same hardware.
- **Security**: "Chiseled" images have no shell (`/bin/sh`), minimizing attack surface.
- **Startup Time**: Native AOT starts in milliseconds, critical for scaling.

<ExternalCite title=".NET Container Images" url="https://learn.microsoft.com/en-us/dotnet/core/docker/container-images" author="Microsoft" date="2024-11-12" />

<ExternalCite title="Ubuntu Chiselled Containers" url="https://ubuntu.com/blog/chiselled-ubuntu-for-dotnet" author="Canonical" date="2024-03-15" />

### What We'll Build

We will transform our Dockerfile from a standard 300MB image to a highly optimized **30MB** image using multi-stage builds and Chiseled Ubuntu.

## Architecture Overview

We rely on Microsoft's "Chiseled" images—stripped-down versions of Ubuntu designed solely for running an app, no administration tools included.

## Section 1: The Multi-Stage Build

We compile in a full SDK container, but publish to a minimal runtime.

```dockerfile
# Build Stage
FROM mcr.microsoft.com/dotnet/sdk:10.0 AS build
WORKDIR /src
COPY . .
RUN dotnet publish "src/MyApp.Api" -c Release -o /app/publish /p:UseAppHost=false

# Runtime Stage (Chiseled)
FROM mcr.microsoft.com/dotnet/aspnet:10.0-noble-chiseled
WORKDIR /app
COPY --from=build /app/publish .
ENTRYPOINT ["dotnet", "MyApp.Api.dll"]
```

This simple change drops the image size from **~250MB** (Debian default) to **~90MB**.

<ImplementationNote title="Chiseled images and debugging in production">
The first time I deployed a Chiseled image to my cluster, I immediately regretted it -- because when a pod started crash-looping, I could not `kubectl exec` into it and run `bash` or even `sh`. There is no shell in the container at all. I had to pivot to a completely different debugging workflow: ephemeral debug containers (`kubectl debug -it <pod> --image=busybox --target=api`) and structured logging piped to Loki. It took me a full day to adjust my muscle memory, but the security tradeoff is absolutely worth it. No shell means no reverse shell if something gets compromised. I now keep a small debug container image pre-pulled on every node for exactly these situations.
</ImplementationNote>

## Section 2: Native AOT (Ahead-of-Time)

For our Worker services (which process queues), we can go further. Native AOT compiles the C# code directly to machine code, removing the need for the JIT compiler and part of the runtime.

<ExternalCite title="Native AOT Deployment" url="https://learn.microsoft.com/en-us/dotnet/core/deploying/native-aot/" author="Microsoft" date="2024-11-12" />

**Project File Changes:**

```xml
<PropertyGroup>
    <PublishAot>true</PublishAot>
    <InvariantGlobalization>true</InvariantGlobalization>
</PropertyGroup>
```

**Dockerfile for AOT:**

```dockerfile
# Runtime Stage (Deps only)
FROM mcr.microsoft.com/dotnet/runtime-deps:10.0-noble-chiseled-aot
WORKDIR /app
COPY --from=build /app/publish .
ENTRYPOINT ["./MyApp.Workers"]
```

**Result:**
- **Image Size**: ~25MB
- **Startup Time**: 15ms
- **Memory Footprint**: ~18MB RAM (idle)

<Callout type="warning" title="AOT Limitations">
    Native AOT restricts reflection and dynamic code generation. Some libraries (like older Newtonsoft.Json or EF Core without pre-compilation) may not work out of the box. Test thoroughly.
</Callout>

<ImplementationNote title="The System.Text.Json source generator saved AOT for us">
My first attempt at Native AOT compilation failed spectacularly. The NATS worker used `System.Text.Json` with reflection-based serialization, and AOT stripped all the type metadata at compile time. Every message deserialized to null fields. I did not realize the issue until I saw the worker process messages with zero data and silently ack them. The fix was switching to `System.Text.Json` source generators with `[JsonSerializable]` attributes on every DTO. It required touching about 40 files, but the result was a worker that compiled to AOT cleanly, started in 15ms, and used only 18 MB of RAM at idle. I now enforce source generators via an analyzer rule in our `Directory.Build.props` so nobody accidentally regresses.
</ImplementationNote>

## Section 3: Tree Shaking

Even without AOT, .NET 10 performs "trimming" (Tree Shaking) during publish. It analyzes your code and removes unused classes from the System libraries.

<ExternalCite title="Trim self-contained deployments and executables" url="https://learn.microsoft.com/en-us/dotnet/core/deploying/trimming/trim-self-contained" author="Microsoft" date="2024-11-12" />

We ensure this is enabled in our `Directory.Build.props`:

```xml
<PropertyGroup>
    <PublishTrimmed>true</PublishTrimmed>
    <TrimMode>partial</TrimMode>
</PropertyGroup>
```

## Conclusion

By caring about our artifacts, we reduced the total memory footprint of our application layer by **60%**. This allows us to allocate more RAM to where it's actually needed: the Database and Vector Index.

<ExternalCite title="Best practices for writing Dockerfiles" url="https://docs.docker.com/develop/develop-images/dockerfile_best-practices/" author="Docker" date="2024-10-01" />

The optimization work described here turned my cluster from a fragile, OOMKill-prone system into something genuinely stable. Before these changes, I could barely run 10 pods on a single 8 GB node; after Chiseled images and AOT, I comfortably run 20+ pods with headroom to spare. The most surprising benefit was not the memory savings alone but the startup speed: AOT workers restart in under 20ms, which makes rolling deployments essentially invisible to clients. If you are running a homelab or any resource-constrained Kubernetes environment, image optimization is not a nice-to-have -- it is the difference between a cluster that works and one that constantly fights you.

### Next Steps

- Verify performance gains with [Benchmarking](/microservices-benchmarking-and-stress-testing).
- See how this enables faster [Contract Testing](/pact-consumer-driven-contract-testing) pipelines.
- Profile memory usage over time with Prometheus and Grafana to validate real-world savings.
- Experiment with Alpine-based images for non-.NET services to achieve similar size reductions.

### Further Reading

- [Microsoft .NET Container Images Documentation](https://learn.microsoft.com/en-us/dotnet/core/docker/container-images) -- Official guidance on choosing the right base image for .NET containers.
- [Ubuntu Chiselled Containers](https://ubuntu.com/blog/chiselled-ubuntu-for-dotnet) -- Canonical's explanation of Chiselled images and their security benefits.
- [Native AOT Deployment Overview](https://learn.microsoft.com/en-us/dotnet/core/deploying/native-aot/) -- Deep dive into AOT compilation trade-offs, limitations, and best practices.
