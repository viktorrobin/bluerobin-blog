## Summary

<!-- What changes and why. Keep to 1-3 bullets. -->

## Requirements citation

Implements: `<FR-N | NFR-N | SEC-N | OBS-N | PRIV-N | HS-N | RT-N | SLO-N | IP-N | TR-N | TS-N | FEAT-NNN>`

<!--
At least one requirement ID is REQUIRED. CI gate (`requirements-citation.yml`)
fails the PR otherwise.

Bypass: apply the `requirements-exempt` label with Lead-of-affected-service
approval. The override is recorded for SOC 2 audit (≥ 1 year retention per
`SLO-EB-9`).
-->

## Test plan

- [ ] <!-- one bullet per scenario verified locally or in CI -->
